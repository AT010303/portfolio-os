import styled from 'styled-components';

const StyledClock = styled.nav`
  height: 100%;
  width: 15%;
  position: absolute;
  right: 0;
  bottom: 0;
  background-color: #999;
`;

export default StyledClock;
